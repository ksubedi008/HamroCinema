import React, { useState, useEffect } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { Link } from 'react-router-dom'; const PreferencesWidget = () => { const [isVisible, setIsVisible] = useState(false); useEffect(() => { try { const consent = localStorage.getItem('sitePreferencesConsent'); if (!consent) { setIsVisible(true); } } catch (e) { console.warn("Storage access restricted by browser."); } }, []); const handleAccept = () => { try { localStorage.setItem('sitePreferencesConsent', 'true'); } catch (e) {} setIsVisible(false); }; const handleDecline = () => { try { localStorage.setItem('sitePreferencesConsent', 'false'); } catch (e) {} setIsVisible(false); }; return ( <AnimatePresence> {isVisible && ( <motion.div initial={{ y: 100, opacity: 0 }} animate={{ y: 0, opacity: 1 }} exit={{ y: 100, opacity: 0, transition: { duration: 0.3 } }} transition={{ type: 'spring', damping: 25, stiffness: 120 }} className="fixed bottom-4 left-4 right-4 md:left-8 md:right-auto md:w-[400px] z-50 p-6 bg-[#1A1A1A] border border-neutral-800 rounded-2xl shadow-lg" > <div className="flex flex-col gap-4"> <div> <h3 className="text-neutral-100 font-bold text-lg mb-2 flex items-center gap-2"> 🛡️ Site Preferences </h3> <p className="text-neutral-300 text-sm leading-relaxed"> HamroCinema uses basic diagnostic data to enhance your browsing experience and serve personalized content. Read more in our <Link to="/privacy-policy" className="text-neutral-400 hover:text-neutral-100 transition-colors duration-200 ease-in-out underline underline-offset-4">Privacy Policy</Link>. </p> </div> <div className="flex items-center gap-3 mt-1"> <button onClick={handleAccept} className="btn-premium flex-1 py-2.5 px-4 text-sm rounded-xl" > Accept All </button> <button onClick={handleDecline} className="flex-1 py-2.5 px-4 bg-transparent border border-neutral-800 hover:border-neutral-800 text-neutral-400 hover:text-gray-200 text-sm font-medium rounded-xl transition-all duration-200 ease-in-out" > Decline </button> </div> </div> </motion.div> )} </AnimatePresence> );
}; export default PreferencesWidget;
