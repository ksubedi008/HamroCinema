import React, { useState, useContext } from 'react';
import axios from 'axios';
import { AuthContext } from '../../context/AuthContext';
import { motion } from 'framer-motion';
import AlertMessage from '../../components/AlertMessage';

const ChangePassword = () => { const { authTokens } = useContext(AuthContext); const [formData, setFormData] = useState({ old_password: '', new_password: '', confirm_password: '' }); const [isSubmitting, setIsSubmitting] = useState(false); const [message, setMessage] = useState(''); const [error, setError] = useState(''); const handleInputChange = (e) => { setFormData({ ...formData, [e.target.name]: e.target.value }); }; const handleSubmit = async (e) => { e.preventDefault(); setMessage(''); setError(''); if (formData.new_password !== formData.confirm_password) { setError("New Password and Confirm New Password do not match."); return; } if (formData.new_password.length < 8) { setError("New password must be at least 8 characters long."); return; } setIsSubmitting(true); try { const response = await axios.put(`${import.meta.env.VITE_API_BASE_URL}/api/users/me/password/`, { old_password: formData.old_password, new_password: formData.new_password }, { headers: { 'Authorization': `Bearer ${authTokens.access}` } }); setMessage(response.data.message || 'Password updated successfully!'); setFormData({ old_password: '', new_password: '', confirm_password: '' }); } catch (err) { console.error('Error updating password:', err); setError(err.response?.data?.error || 'Failed to update password. Please check your current password.'); } finally { setIsSubmitting(false); } }; return ( <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="min-h-screen pt-24 pb-12 px-4 sm:px-6 lg:px-8 max-w-2xl mx-auto" > <div className="bg-[#1A1A1A] border border-neutral-800 rounded-2xl shadow-xl overflow-hidden"> <div className="px-6 py-8 sm:p-10"> <h2 className="text-3xl font-black text-neutral-100 mb-2 border-l-4 border-neutral-600 pl-4">Change Password</h2> <p className="text-neutral-400 mb-8 pl-4">Update your password to keep your account secure.</p> {message && ( <div className="mb-6"><AlertMessage message={message} type="success" /></div> )} {error && ( <div className="mb-6"><AlertMessage message={error} type="error" /></div> )} <form onSubmit={handleSubmit} className="space-y-6"> <div> <label className="block text-sm font-medium text-neutral-400 mb-2">Current Password</label> <input type="password" name="old_password" value={formData.old_password} onChange={handleInputChange} className="w-full bg-[#121212] border border-neutral-800 rounded-xl px-4 py-3 text-neutral-100 placeholder-zinc-600 focus:outline-none focus:border-neutral-600 focus:ring-1 focus:ring-white/20 transition-all duration-200 ease-in-out" placeholder="Enter your current password" required /> </div> <div className="pt-4 border-t border-neutral-800/50"> <label className="block text-sm font-medium text-neutral-400 mb-2">New Password</label> <input type="password" name="new_password" value={formData.new_password} onChange={handleInputChange} className="w-full bg-[#121212] border border-neutral-800 rounded-xl px-4 py-3 text-neutral-100 placeholder-zinc-600 focus:outline-none focus:border-neutral-600 focus:ring-1 focus:ring-white/20 transition-all duration-200 ease-in-out" placeholder="Enter a new secure password" required /> </div> <div> <label className="block text-sm font-medium text-neutral-400 mb-2">Confirm New Password</label> <input type="password" name="confirm_password" value={formData.confirm_password} onChange={handleInputChange} className="w-full bg-[#121212] border border-neutral-800 rounded-xl px-4 py-3 text-neutral-100 placeholder-zinc-600 focus:outline-none focus:border-neutral-600 focus:ring-1 focus:ring-white/20 transition-all duration-200 ease-in-out" placeholder="Confirm your new password" required /> </div> <div className="pt-6"> <button type="submit" disabled={isSubmitting} className={`w-full py-4 rounded-xl flex items-center justify-center font-bold text-sm uppercase tracking-widest transition-all duration-200 ease-in-out ${isSubmitting ? 'bg-[#1A1A1A] text-zinc-500 cursor-not-allowed' : 'bg-white text-black hover:bg-gray-200 shadow-lg'}`} > {isSubmitting ? ( <svg className="w-5 h-5 animate-spin text-zinc-500" fill="none" viewBox="0 0 24 24"> <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle> <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path> </svg> ) : ( 'Update Password' )} </button> </div> </form> </div> </div> </motion.div> );
}; export default ChangePassword;
